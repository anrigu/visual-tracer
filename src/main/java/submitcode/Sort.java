package submitcode;

/**
 * An interface of a sorting algorithm
 *
 * @author Anri Gu
 * @version 1.0
 * @since 1.0
 *
 */
public interface Sort {

  /**
   * Sorts the given simple array
   *
   * @param simpleArray - An unsorted simple array
   */
  void sort(SimpleArray simpleArray);
}
